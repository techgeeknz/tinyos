# HP_TOOLS sysfs overrides; applied after HP_TOOLS is mounted
# comments ok; paths are relative to /sys unless absolute
module/firmware_class/parameters/path = /lib/firmware
#devices/system/cpu/cpu0/cpufreq/scaling_governor = performance
